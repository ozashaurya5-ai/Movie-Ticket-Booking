// Dropdown Toggle
function toggleDropdown() {
  const dropdown = document.getElementById("dropdown-content");
  dropdown.style.display = (dropdown.style.display === "block") ? "none" : "block";
}

// Hide dropdown if clicked outside
window.onclick = function(event) {
  if (!event.target.matches('.dropbtn')) {
    const dropdown = document.getElementById("dropdown-content");
    if (dropdown && dropdown.style.display === "block") {
      dropdown.style.display = "none";
    }
  }
}

// Toggle Button Function
function setToggle(btn) {
  document.querySelectorAll(".toggle-btns button").forEach(b => b.classList.remove("active"));
  btn.classList.add("active");
}

// Booking Function
function bookNow() {
  const movie = document.getElementById("movie").value;
  const date = document.getElementById("date").value;
  const cinema = document.getElementById("cinema").value;
  const time = document.getElementById("time").value;

  if(movie.includes("Select") || date.includes("Select") || cinema.includes("Select") || time.includes("Select")){
    alert("⚠️ Please select all fields before booking!");
  } else {
    alert(`🎬 Booking Confirmed!\nMovie: ${movie}\nDate: ${date}\nCinema: ${cinema}\nTime: ${time}`);
  }
}
